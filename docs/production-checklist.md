# 生产上线检查清单

上线前请逐项确认：

- [ ] 已修改默认管理员密码（`admin@example.com` 不再使用默认口令）。
- [ ] 已配置高强度 `JWT_SECRET`。
- [ ] 已配置真实 `NEWAPI_BASE_URL`。
- [ ] 已配置真实 `NEWAPI_API_KEY`。
- [ ] 已配置 `REDEEM_BUY_URL`。
- [ ] 已在后台完成 NewAPI 连接测试。
- [ ] 已测试兑换码充值流程。
- [ ] 已测试聊天扣费（预扣、结算、多退少补）。
- [ ] 已检查 `BalanceLog` 写入正常。
- [ ] 已检查 `ChatBill` 写入正常。
- [ ] 已完成数据库备份策略（全量+增量）。
- [ ] 已删除或禁用测试兑换码 `TEST-100`。
